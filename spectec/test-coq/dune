(rule
  (targets coq_output_current.v)
  (deps
    (file ../watsup)
    (glob_files ../spec/*.watsup)
  )
  (action
    (progn
      (with-stdout-to %{targets} (run %{deps} --coq))
      (run coqc %{targets})
      (run coqdoc --utf8 --title coq_output %{targets})
    )
  )
)